apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: afi

resources:
  - ../../base

images:
  - name: ghcr.io/aboulmali/afi-backend
    newTag: main

# Secrets à créer manuellement ou via ExternalSecret/SealedSecret :
#   kubectl -n afi create secret generic afi-secrets \
#     --from-literal=database_url='postgresql://afi:...' \
#     --from-literal=openai_api_key='...'
#     --from-literal=secret_key='...'